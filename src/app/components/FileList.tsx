'use client'

import React from 'react'
// import { useAppContext } from '../context/AppContext'

const FileList: React.FC = () => {
  // const { cueFiles } = useAppContext()

  return (
    <div>
      {/* <h2 className="text-xl font-semibold mb-2">Found .cue Files:</h2>
      {cueFiles.length === 0 ? (
        <p>No .cue files found</p>
      ) : (
        <ul className="list-disc pl-5">
          {cueFiles.map((file, index) => (
            <>hahaha</>
            // <li key={index} className="mb-1">{file}</li>
          ))}
        </ul>
      )} */}
    </div>
  )
}

export default FileList

